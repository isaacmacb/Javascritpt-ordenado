const cores = ['vermelho', 'azul', 'verde'];

for(const cor of cores) {
    console.log(cor);
}

const pessoas = {
    name: 'Jhon',
    idade: 28,
    cidade: 'fortaleza'
}
    for(const key in pessoas) {
        console.log();
    }